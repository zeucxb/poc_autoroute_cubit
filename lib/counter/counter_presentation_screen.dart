import 'package:flutter/material.dart';

class CounterPresentationScreen extends StatelessWidget {
  const CounterPresentationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Presentation'),
      ),
      body: Container(),
    );
  }
}
